require "application_system_test_case"

class BlogsTest < ApplicationSystemTestCase
  # test "visiting the index" do
  #   visit blogs_url
  #
  #   assert_selector "h1", text: "Blog"
  # end
  
  test "Anyone can get list of tags for existing blog." do
    # blog_hehe = FactoryBot.create(:blog)
    # 1. Open get_tags page with blog identifier. 
    # visit get_tags_url(blog_hehe)
    # 2. See list of all tags with links to their subpages.

    # visit blogs_url
  
    # assert_selector "h1", text: "Blog"
  end
end
